export const listViewConfig = {
  defaultListViewStyle: false, // If set to true, the app will have list view by default otherwise it'll have grid view.(true > List View, false > Grid view)
  enableUserControl: true, // If set to true, user will be able to change the view styles between list view and grid view otherwise the view style will be fixed to default style
};
